server:
  port: 8912

minio:
  endpoint: http://localhost:9001
  accessKey: driftwood
  secretKey: driftwood

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 512MB # 单个文件的最大上限
      max-request-size: 512MB # 单个请求的最大上限
