package com.icuxika.everything.warp.exchange.trading.api;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.icuxika")
public class WarpExchangeTradingApiApplication {

    public static void main(String[] args) {
        SpringApplication.run(WarpExchangeTradingApiApplication.class, args);
    }
}
